import React from "react";
import { MembersResult, UserType } from "../types/types";
type AvatarProps = {
    id?: number;
    name?: string;
    src?: string;
    presence?: string;
    size?: number;
    className?: string;
};
export declare const Avatar: ({ id, src, name, presence, size, className, }: AvatarProps) => React.JSX.Element;
type AvatarGroupProps = {
    user: UserType;
    name?: string;
    members?: MembersResult;
    size?: number;
    className?: string;
};
export declare const AvatarGroup: ({ user, members, name, size, }: AvatarGroupProps) => React.JSX.Element;
export default Avatar;
