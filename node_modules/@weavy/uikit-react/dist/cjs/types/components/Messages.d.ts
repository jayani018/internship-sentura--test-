import React from "react";
import { AppFeatures, MembersResult } from "../types/types";
type Props = {
    id: number;
    members: MembersResult | undefined;
    displayName?: string;
    avatarUrl?: string;
    lastMessageId: number | null;
    chatRoom: boolean;
    contextual?: boolean;
    features: string[];
    appFeatures: AppFeatures | undefined;
};
declare const Messages: ({ id, members, displayName, avatarUrl, lastMessageId, chatRoom, contextual, features, appFeatures, }: Props) => React.JSX.Element;
export default Messages;
