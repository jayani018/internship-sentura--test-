import React from "react";
type Props = {
    onMeetingAdded: Function;
    dropdown?: boolean;
};
declare const Meetings: ({ onMeetingAdded, dropdown }: Props) => React.JSX.Element;
export default Meetings;
