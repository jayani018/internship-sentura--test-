import React from 'react';
import { FilesProps } from '../types/Files';
declare const Files: ({ uid, className, view: initView, order: initOrder, trashed: initTrashed, features }: FilesProps) => React.JSX.Element;
export default Files;
