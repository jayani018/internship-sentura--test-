import React from "react";
type Props = {
    id: number;
    name: string;
    className?: string;
    children?: React.ReactNode;
};
declare const Blob: ({ id, name, className, children }: Props) => React.JSX.Element;
export default Blob;
